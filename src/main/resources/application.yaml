spring:
  profiles:
    active: local
  application:
    name: product
  threads:
    virtual:
      enabled: true
  data:
    mongodb:
      uri: mongodb://localhost:27017/sotti-produc
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

app:
  admin-api-key: ${ADMIN_API_KEY:sotti:63C5942B-9FB6-4D65-995A-100E6CF8D5DF}
  admin-password: ${ADMIN_PASSWORD:sotti-admin-2026}
  r2:
    access-key: ${R2_ACCESS_KEY:}
    secret-key: ${R2_SECRET_KEY:}
    endpoint: ${R2_ENDPOINT:}
    bucket: ${R2_BUCKET:}
    public-base-url: ${R2_PUBLIC_BASE_URL:}

http:
  timeout:
    default: 30000